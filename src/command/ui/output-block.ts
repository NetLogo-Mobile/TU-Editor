import { UIRenderer } from "./ui-renderer";

/** OutputBlock: A block that displays the output of a command. */
export class OutputBlock extends UIRenderer {
    /** RenderInternal: Render the UI element. */
    protected RenderInternal(): void {
    }
}